# @package _global_

defaults:  
  - override /data: ??
  - override /model: concat_naive
  - _self_

tags: ["diff_naive"]
task_name: "train_test"

data:
  params:
    prompt_type: "sample"
    n_prompt: 1
    n_shape: 3
    loc: "center"
    batch_size: 2
    num_worker: 2 # link to trainer
    pin_memory: False