# @package _global_

defaults:  
  - override /data: ??
  - override /model: diff
  - _self_

tags: ["train"]
task_name: "train_probing_diff"

data:
  params:
    prompt_type: "sample"
    n_prompt: 1
    n_shape: 3
    loc: "center"

model:
  instance:
    params:
      ft_mode: "probing"