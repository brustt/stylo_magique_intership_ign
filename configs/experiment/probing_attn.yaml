# @package _global_

defaults:  
  - override /data: ??
  - override /model: attn
  - _self_

tags: ["train"]
task_name: ??

data:
  params:
    prompt_type: "sample"
    n_prompt: 1
    n_shape: 3
    loc: "center"

model:
  instance:
    params:
      ft_mode: "probing"